spring:
  application:
    name: bidmatch
  profiles:
    default: local

server:
  port: 8080
  servlet:
    session:
      timeout: 30m
      cookie:
        name: BIDMATCH_SESSION
        http-only: true

logging:
  level:
    com.project.bidmatch: DEBUG